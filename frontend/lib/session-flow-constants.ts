/** 투표 진행 시간(초) — 서버와 동일 값 유지 */
export const VOTE_DURATION_SEC = 10 * 60;
/** 서버 `finalize`의 CREATOR_FINALIZE_LEAD_MS와 동일 */
export const CREATOR_FINALIZE_LEAD_MS = 10 * 60 * 1000;
/** Supabase Realtime 연속 이벤트 시 REST 재조회 디바운스(ms) */
export const REALTIME_REFETCH_DEBOUNCE_MS = 400;
